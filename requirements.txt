# mt5_quant_trading_agent dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: MetaTrader5 is a Windows-only package and requires the MT5 terminal
# installed. The pure-logic modules (risk, recovery, strategy, backtester,
# optimizer) and the unit tests run WITHOUT MetaTrader5 installed.

MetaTrader5>=5.0.45 ; platform_system == "Windows"
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
pytest>=7.4
